careersignal_dbt:
  target: local
  outputs:
    local:
      type: duckdb
      path: "../data/local/careersignal.duckdb"
      schema: main
      threads: 4

    dev:
      type: duckdb
      path: "md:{{ env_var('MOTHERDUCK_DATABASE', 'CareerSignal') }}?motherduck_token={{ env_var('MOTHERDUCK_TOKEN') }}"
      schema: main
      threads: 4
