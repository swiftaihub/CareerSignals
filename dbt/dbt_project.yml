name: careersignal_dbt
version: "1.0.0"
config-version: 2

profile: careersignal_dbt

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  careersignal_dbt:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: mart
